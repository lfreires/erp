<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Relatórios - <%= obra.obraNome %></title>
  <style>
    body { font-family: sans-serif; padding: 20px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 10px; border: 1px solid #ccc; }
    th { background: #f2f2f2; }
  </style>
</head>
<body>
  <h1>Relatórios da <%= obra.obraNome %></h1>
  <table>
    <thead>
      <tr>
        <th>Data</th>
        <th>Responsável</th>
        <th>Descrição</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% obra.relatorios.forEach(r => { %>
        <tr>
          <td><%= new Date(r.data).toLocaleDateString() %></td>
          <td><%= r.usuario %></td>
          <td><%= r.descricao %></td>
          <td><a href="/obra/<%= obra.obraId %>/relatorio/<%= r.id %>">Ver</a></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
